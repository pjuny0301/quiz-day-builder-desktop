[project]
name = "quiz-day-builder"
version = "0.1.0"
description = "PyQt quiz app with deck/day management"
requires-python = ">=3.11"
dependencies = [
  "PyQt6>=6.8.0",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
